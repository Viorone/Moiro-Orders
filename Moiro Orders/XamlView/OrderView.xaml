<UserControl x:Class="Moiro_Orders.XamlView.OrderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:Moiro_Orders.XamlView"
      xmlns:ViewModel="clr-namespace:Moiro_Orders.ViewModel"
      mc:Ignorable="d" 
 
     >
    <UserControl.Resources>
        <ViewModel:OrderViewModel x:Key="ViewModelOrder"/>
    </UserControl.Resources>
    <Grid Margin="5" 
           DataContext="{StaticResource ViewModelOrder}"
          Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="210*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="155"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="1" 
                 Grid.Row="1" 
                 Grid.RowSpan="2"
                ItemsSource="{Binding Orders}"
                 x:Name="listOrders">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock FontSize="18" Text="{Binding Path=Problem}" />
                        <TextBlock Text="{Binding Path=Description}" />
                        <TextBlock Text="{Binding Path=Date}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Column="1" Orientation="Horizontal"  Grid.Row="0" >

            <TextBlock Text="Выберите дату:" Height="50" Margin="0,30,30,0" FontSize="18" FontFamily="Roboto"  FontWeight="Bold" />
            <DatePicker  x:Name="datePick" Width="auto" Height="40" FontSize="18" FontFamily="Arial" SelectedDateFormat="Long" SelectedDate="{Binding ShowMessageBox}" HorizontalAlignment="Left" Background="Indigo">
               
            </DatePicker>

            <Button  Content="CLICK!!!!" Command="{Binding CommandGetOrdersOfDate}"></Button>
        </StackPanel>
        <!--
         <Button  Grid.Column="1" Grid.Row="0" Content="Ткущая дата и надо свзять с календарём"
                 Command="{Binding CommandGetOrdersOfDate}"
                 Click="Button_Click" Height="Auto"/>
            -->
        <Button Grid.Row="1" Content="Добавить заявку" x:Name="addAllOrders" Click="AddAllOrders_Click" Height="Auto" Margin="5" Background="Indigo" Foreground="AliceBlue" BorderBrush="{x:Null}"/>
        <Button Grid.Row="2" Content="Отменить заявку"  Margin="5" Background="Indigo" Foreground="AliceBlue" BorderBrush="{x:Null}" Height="Auto"/>
        
    </Grid>
</UserControl>
  
